export default {
    fetchedList(state) {
        return state.list;
    },
    fetchedUserList(state) {
        return state.userList;
    },
    fetchedItemList(state) {
        return state.itemList;
    },
}